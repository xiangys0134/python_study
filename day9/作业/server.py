#!/usr/bin/python
# -*- coding: utf-8 -*-

from multiprocessing import Process
import os
import socket
import time
import os

server = socket.socket()
server.bind(('127.0.0.1',8000))
server.bind(5)

class Myserver:
    def __init__(self):
        pass

    while True:
